"""tests — WDA 工具测试模块。

测试框架：基于 unittest.TestCase，兼容 pytest 运行。

用法：
    # 运行所有测试
    cd DAgent && python3 -m pytest tests/ -v

    # 运行单个测试文件
    cd DAgent && python3 -m pytest tests/test_tools.py -v

    # 直接运行（不需要 pytest）
    cd DAgent && python3 tests/test_tools.py
"""
